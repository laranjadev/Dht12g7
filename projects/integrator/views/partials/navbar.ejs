<% if (typeof isUserSession === 'undefined') { } else { %>
  <%
    let isUserData = '';
    isUserData += isUserSession['id'] ? '<p>' + getFirstUpperCase(isUserSession['id']) + '</p>' : '';
    isUserData += isUserSession['title'] ? '<p>' + getFirstUpperCase(isUserSession['title']) + '</p>' : '';
    isUserData += isUserSession['accesskey'] ? '<p>' + getFirstUpperCase(isUserSession['accesskey']) + '</p>' : '';
    isUserData += isUserSession['phone'] ? '<p>' + getFirstUpperCase(isUserSession['phone']) + '</p>' : ''; 
  %>
  <div id="content"><%- isUserData; %></div>
<% } %>
<%- bootstrapNavbar({
  key : typeof key !== 'undefined' ? key : undefined,
  searchAction : typeof isSearchAction !== 'undefined' ? isSearchAction : undefined,
  array : isNavbar,
  element : 'nav',
  id : 'navbar',
}); %>
<%- typeof isPageTitle !== 'undefined' ? '<h1>' + getFirstUpperCase(isPageTitle) + '</h1>' : ''; %>
<div id="content">