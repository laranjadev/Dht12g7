import {
    quickviewClasses,
} from './classes.js';

quickviewClasses({
    color : 'dark',
    position : 'end',
    style : 'outline',
});

import {
    headerClasses,
} from './classes.js';

headerClasses();

import {
    paginationClasses,
} from './classes.js';

paginationClasses({
    position : 'center',
});