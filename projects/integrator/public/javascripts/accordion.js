import {
    quickviewClasses,
} from './classes.js';

quickviewClasses({
    color : 'dark',
    position : 'center',
    style : 'outline',
});

import {
    headerClasses,
} from './classes.js';

headerClasses();

import {
    accordionClasses,
} from './classes.js';

accordionClasses();